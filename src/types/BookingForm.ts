export interface BookingFormData {
  name: string;
  tel: string;
  email: string;
  address: string;
}

export interface BookingFormErrors {
  name: string;
  tel: string;
  email: string;
  address: string;
}
