# Copy this file to .env.local and adjust for your machine.
#
# Leave empty during local development — Vite proxies /api to the backend
# (see vite.config.ts). In production set it to the deployed backend URL,
# e.g. https://team-project-backend.onrender.com
VITE_API_URL=
